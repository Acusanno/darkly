Lorsqu'on va sur la page survey, on peut choisir un nombre entre 1 et 10, cela envoie le nombre de vote correspondant.

On peut inspecter la page pour voir que chaque input a une variable 'value' qui determine combien de votes est envoyer au back.

Il suffit de modifier un input avec une grande valeur (999999) et de voter pour faire apparaitre le flag.