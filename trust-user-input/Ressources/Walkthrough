Lorsqu'on va sur la page survey, on peut choisir un nombre entre 1 et 10, cela envoie le nombre de votes correspondant.

On peut inspecter la page pour voir que chaque input a une variable 'value' qui détermine combien de votes est envoyé au back.

Il suffit de modifier un input avec une grande valeur (999999) et de voter pour faire apparaître le flag.

Solution :

Il faut avoir une validation côté back, la validation côté front uniquement ne suffit pas.